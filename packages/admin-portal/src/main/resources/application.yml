spring:
  application:
    name: admin-portal

server:
  port: 8080

jwt:
  public-key: ${JWT_PUBLIC_KEY}
  private-key: ${JWT_PRIVATE_KEY}

app:
  security:
    public-paths:
      - /actuator/health

spring:
  application:
    name: notification-service
  config:
    import: optional:configserver:${CONFIG_SERVER_URL:http://localhost:8888}
  data:
    mongodb:
      uri: ${MONGO_URI:mongodb://localhost:27017/chatapp_notifications}
  kafka:
    consumer:
      group-id: notification-service

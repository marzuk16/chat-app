spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:${user.dir}/../../infrastructure/config-repo

management:
  endpoints:
    web:
      exposure:
        include: health, info

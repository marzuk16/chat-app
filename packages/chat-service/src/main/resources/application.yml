spring:
  application:
    name: chat-service
  config:
    import: optional:configserver:${CONFIG_SERVER_URL:http://localhost:8888}
  data:
    mongodb:
      uri: ${MONGO_URI:mongodb://localhost:27017/chatapp_messages}
  kafka:
    consumer:
      group-id: chat-service
